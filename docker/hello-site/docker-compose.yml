services:
  web:
    build: .
    container_name: hello-web
    ports: 
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro


  redis:
    image: redis:alpine
    container_name: hello-redis
    volumes:
       - redis-data:/data


volumes:
  redis-data:

name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5


      - name: Say hello
        run: echo "Hello from CI — pushed by Fahad"

      - name: Show directory
        run: ls -la
